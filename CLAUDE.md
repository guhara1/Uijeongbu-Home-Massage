# 작업 지침 (88마사지 / 의정부 출장마사지 사이트)

## Git / 머지 정책
- **항상 자동 머지**: 작업 완료 시 PR 생성·머지 여부를 사용자에게 묻지 않고 자동으로 처리한다.
- 이 저장소의 기본 브랜치는 `claude/busy-brown-pder8h`이며, 개발·푸시도 이 브랜치로 한다.
  따라서 이 브랜치로 push 하면 곧바로 기본 브랜치(배포본)에 반영된다 — 별도 머지 단계 불필요.
- 만약 향후 `main` 등 별도 기본 브랜치가 생기면, 작업 브랜치를 기본 브랜치로 자동 머지한다(묻지 않음).
- 커밋·푸시는 작업이 끝나면 곧바로 수행한다.

## 빌드
- 정적 사이트는 `python3 build.py`로 생성하며, 결과물(HTML/sitemap/robots)은 저장소 루트에 출력되어 그대로 배포된다.
- 빌드 후 색인 페이지 본문 ≥2,000자, JSON-LD 유효성, 내부링크 정합성을 확인한다.

## 콘텐츠 원칙
- 상호 **88마사지**, 전화예약 **0508-202-4719**, 메타 디스크립션 80자 이내.
- 메뉴·URL에 키워드("출장마사지") 반복 금지, 가짜 후기·체험담 금지.
- 모든 페이지 본문은 고유 작성(지역명 단순치환 금지).
